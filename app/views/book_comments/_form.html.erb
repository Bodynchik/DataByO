<%= form_with(model: book_comment) do |form| %>
  <% if book_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book_comment.errors.count, "error") %> prohibited this book_comment from being saved:</h2>

      <ul>
        <% book_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :book_id, style: "display: block" %>
    <%= form.text_field :book_id %>
  </div>

  <div>
    <%= form.label :client_id, style: "display: block" %>
    <%= form.text_field :client_id %>
  </div>

  <div>
    <%= form.label :comment_text, style: "display: block" %>
    <%= form.text_field :comment_text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
