<%= form_with(model: book_raiting) do |form| %>
  <% if book_raiting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book_raiting.errors.count, "error") %> prohibited this book_raiting from being saved:</h2>

      <ul>
        <% book_raiting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :book_id, style: "display: block" %>
    <%= form.text_field :book_id %>
  </div>

  <div>
    <%= form.label :client_id, style: "display: block" %>
    <%= form.text_field :client_id %>
  </div>

  <div>
    <%= form.label :rating_value, style: "display: block" %>
    <%= form.text_field :rating_value %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
